import fs from 'node:fs';
import assert from 'node:assert/strict';
const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');
const pkg=JSON.parse(read('package.json'));
assert.equal(pkg.version,'0.24.0');
assert.equal(pkg.dependencies['capacitor-secure-storage-plugin'],'0.12.0');
const cloud=read('src/cloud.js'),app=read('src/app.js');
for(const token of ['SecureStoragePlugin','initializePosSessionStorage','SESSION_CACHE','OPERATOR_CACHE','localStorage.removeItem(SESSION_KEY)'])assert.ok(cloud.includes(token),token);
assert.ok(app.includes('await initializePosSessionStorage()'));
assert.doesNotMatch(cloud,/service[_-]?role/i);
console.log('ReMaPro POS 0.24 smoke checks passed');
