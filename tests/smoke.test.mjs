import fs from 'node:fs';
import assert from 'node:assert/strict';
const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');
const pkg=JSON.parse(read('package.json'));
assert.equal(pkg.version,'0.22.0');
const app=read('src/app.js');
for(const token of ['syncView','syncActionLabel','sync-retry','last_error','attempts:Number','OFFLINE_OPERATOR_REAUTH'])assert.ok(app.includes(token),token);
assert.ok(app.includes("state.view==='sync'"));
assert.ok(read('src/db.js').includes('SQLiteConnection'));
assert.doesNotMatch(read('src/cloud.js'),/service[_-]?role/i);
console.log('ReMaPro POS 0.22 smoke checks passed');
