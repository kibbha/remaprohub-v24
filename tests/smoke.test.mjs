import fs from 'node:fs';
import assert from 'node:assert/strict';
const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');
const pkg=JSON.parse(read('package.json'));
assert.equal(pkg.version,'0.21.0');
const app=read('src/app.js');
for(const token of ['queuedOperatorContext','assertQueuedOperator','append_order_items','send_to_production','update_production_item','applyLocalProductionSend','Statut production enregistré hors ligne'])assert.ok(app.includes(token),token);
assert.ok(app.includes("OFFLINE_OPERATOR_REAUTH"));
assert.ok(read('src/db.js').includes('SQLiteConnection'));
assert.ok(read('src/printer.js').includes('BluetoothPrinter'));
assert.doesNotMatch(read('src/cloud.js'),/service[_-]?role/i);
console.log('ReMaPro POS 0.21 smoke checks passed');
