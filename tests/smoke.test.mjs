import fs from 'node:fs';
import assert from 'node:assert/strict';
const read=p=>fs.readFileSync(new URL('../'+p,import.meta.url),'utf8');
const pkg=JSON.parse(read('package.json'));
assert.equal(pkg.version,'0.23.0');
const app=read('src/app.js');
for(const token of ["kvGet('identity')","kvSet('identity'","openCachedIdentity","Mode hors ligne","logoutPos","kvDelete('identity')"])assert.ok(app.includes(token),token);
assert.ok(app.includes('Première connexion nécessaire'));
assert.ok(app.includes('Un PIN ne peut être revalidé sans serveur'));
assert.ok(read('src/db.js').includes('SQLiteConnection'));
console.log('ReMaPro POS 0.23 smoke checks passed');
