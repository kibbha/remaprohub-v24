import{LANGS,language,setLanguage,t}from'./i18n.js';import{load,save,recordFinance,financeTotals,updateRecord,removeRecord,updatePreferences,exportData,resetState,saveDocument,recordOrder,updateOrder,removeOrder,recordPurchase,updatePurchase,removePurchase}from'./store.js';const modules=['operations','orders','products','haccp','finance','documents','stock','suppliers','purchases','invoices','team','planning','leave','training','recipes','reservations','customers','loyalty','incidents','waste','maintenance','equipment','deliveries','allergens','recalls','cleaning','audits','checklists','alerts','goals','briefing','handover','help'];let page='dashboard',period='day',editing=null;const state=load(),persist=()=>save(state),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),money=n=>new Intl.NumberFormat(language(),{style:'currency',currency:state.preferences?.currency||'CHF'}).format(n||0),today=()=>new Date().toISOString().slice(0,10),actions=(collection,i)=>`<span class="row-actions"><button class="btn compact" data-edit="${collection}" data-index="${i}">${t('edit')}</button><button class="btn danger compact" data-remove="${collection}" data-index="${i}">${t('delete')}</button></span>`,card=(title,body)=>`<section class="card"><h2>${title}</h2>${body}</section>`,rows=(a,fn,empty)=>a.length?a.map(fn).join(''):`<p class="muted">${t(empty)}</p>`,head=title=>`<div class="pagehead">${page!=='dashboard'?`<button class="back" data-page="dashboard" aria-label="${t('back')}">‹</button>`:''}<div><h1>${title}</h1></div></div>`,editingRow=(collection,i)=>editing?.collection===collection&&editing.index===i,editButtons=()=>`<button class="btn primary">${t('save')}</button><button type="button" class="btn" data-cancel-edit>${t('cancel')}</button>`;
function dashboard(){const totals=financeTotals(state,period),avg=totals.covers?totals.revenue/totals.covers:0,h=(state.financeHistory||[]).slice(0,7).reverse(),max=Math.max(1,...h.map(x=>+x.revenue||0)),todo=state.tasks.filter(x=>!x[1]).slice(0,4),low=(state.stock||[]).filter(x=>+x.qty<=+(x.min||0)&&+(x.min||0)>0),urgent=(state.alerts||[]).filter(x=>x.level==='urgent'),pendingInvoices=(state.invoices||[]).filter(x=>x.status!=='paid'),openOrders=(state.orders||[]).filter(x=>x.status!=='paid');return `<section>${head(t('dashboard'))}<p class="muted">${t('dashboardSub')}</p><div class="periods">${['day','week','month'].map(x=>`<button data-period="${x}" class="${period===x?'active':''}">${t(x)}</button>`).join('')}</div><div class="grid">${card(t('revenue'),`<div class="kpi">${money(totals.revenue)}</div><small>${t(period)}</small>${h.length?`<div class="chart">${h.map(x=>`<i style="height:${Math.max(10,(+x.revenue||0)/max*100)}%" title="${esc(x.date)} · ${money(x.revenue)}"></i>`).join('')}</div>`:''}`)}${card(t('covers'),`<div class="kpi">${totals.covers}</div>`)}${card(t('avg'),`<div class="kpi">${money(avg)}</div>`)}${card(t('expenses'),`<div class="kpi">${money(totals.expenses)}</div>`)}</div>${card(t('todayTasks'),todo.length?todo.map(x=>`<div class="priority"><span class="priority-dot"></span><span>${t(x[0])}</span></div>`).join(''):`<p class="muted">✓</p>`)}<div class="grid operational-summary">${card(t('lowStock'),`<div class="kpi mini">${low.length}</div>`)}${card(t('urgentAlerts'),`<div class="kpi mini">${urgent.length}</div>`)}${card(t('pendingInvoices'),`<div class="kpi mini">${pendingInvoices.length}</div>`)}${card(t('openOrders'),`<div class="kpi mini">${openOrders.length}</div>`)}</div><div class="modules">${modules.map(k=>`<button class="module" data-page="${k}"><strong>${t(k)}</strong></button>`).join('')}</div></section>`}function orders(){const total=(state.orders||[]).reduce((s,x)=>s+(+x.amount||0),0);return `<section>${head(t('orders'))}<div class="grid">${card(t('sales'),`<div class="kpi">${money(total)}</div>`)}${card(t('orders'),`<div class="kpi">${state.orders.length}</div>`)}</div>${card(t('newOrder'),`<form id="orderForm" class="form"><input name="reference" required placeholder="${t('orderReference')}"><input name="amount" required type="number" step=".01" placeholder="${t('amount')}"><select name="status"><option value="open">${t('open')}</option><option value="paid">${t('paid')}</option></select><button class="btn primary">${t('add')}</button></form>${rows(state.orders,(x,i)=>editingRow('orders',i)?`<form class="form inline-edit" data-edit-form="orders" data-index="${i}"><input name="reference" required value="${esc(x.reference)}"><input name="amount" required type="number" step=".01" value="${esc(x.amount)}"><select name="status"><option value="open" ${x.status==='open'?'selected':''}>${t('open')}</option><option value="paid" ${x.status==='paid'?'selected':''}>${t('paid')}</option></select>${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.reference)}</strong><br><small class="muted">${t(x.status||'open')}</small></span><span class="row-actions"><strong>${money(x.amount)}</strong>${actions('orders',i)}</span></div>`,'noOrders')}`)}</section>`}function operations(){return `<section>${head(t('operations'))}${card(t('todayTasks'),state.tasks.map((x,i)=>`<label class="row"><span>${t(x[0])}</span><input type="checkbox" data-task="${i}" ${x[1]?'checked':''}></label>`).join(''))}</section>`}function haccp(){return `<section>${head(t('haccp'))}${card(t('temperatureReadings'),`<form id="tempForm" class="form"><input name="equipment" required placeholder="${t('equipment')}"><input name="value" required type="number" step=".1" placeholder="°C"><button class="btn primary">${t('add')}</button></form>${rows(state.temps,(x,i)=>editingRow('temps',i)?`<form class="form inline-edit" data-edit-form="temps" data-index="${i}"><input name="equipment" required value="${esc(x.equipment)}"><input name="value" required type="number" step=".1" value="${esc(x.value)}">${editButtons()}</form>`:`<div class="row"><span>${esc(x.equipment)}</span><span class="row-actions"><strong>${esc(x.value)} °C</strong>${actions('temps',i)}</span></div>`,'noReadings')}`)}</section>`}function finance(){const totals=financeTotals(state,'day');return `<section>${head(t('finance'))}<div class="grid">${card(t('revenue'),`<div class="kpi">${money(totals.revenue)}</div>`)}${card(t('expenses'),`<div class="kpi">${money(totals.expenses)}</div>`)}${card(t('result'),`<div class="kpi">${money(totals.revenue-totals.expenses)}</div>`)}</div>${card(t('dailyEntry'),`<form id="financeForm" class="form"><input name="date" type="date" value="${today()}" required><input name="revenue" type="number" step=".01" value="${state.revenue||''}" placeholder="${t('revenue')}"><input name="covers" type="number" value="${state.covers||''}" placeholder="${t('covers')}"><input name="expenses" type="number" step=".01" value="${state.expenses||''}" placeholder="${t('expenses')}"><button class="btn primary">${t('save')}</button></form>${rows(state.financeHistory.slice(0,7),(x,i)=>`<div class="row"><span>${esc(x.date)}</span><span class="row-actions"><strong>${money(x.revenue)} · ${esc(x.covers)}</strong><button class="btn danger compact" data-remove="financeHistory" data-index="${i}">${t('delete')}</button></span></div>`,'noFinanceHistory')}`)}</section>`}function documents(){const selected=editing?.collection==='document'?editing.type:null;return `<section>${head(t('documents'))}<div class="modules">${state.docs.map(k=>`<article class="card"><strong>${t(k)}</strong><div class="actions"><button class="btn" data-doc-fill="${k}">${t('fill')}</button></div></article>`).join('')}</div>${selected?card(t(selected),`<form id="documentForm" class="form"><input type="hidden" name="type" value="${esc(selected)}"><input name="date" type="date" value="${today()}" required><input name="title" required placeholder="${t('documentTitle')}"><textarea name="details" rows="7" required placeholder="${t('documentDetails')}"></textarea><button class="btn primary">${t('save')}</button><button type="button" class="btn" data-cancel-edit>${t('cancel')}</button></form>`):''}${card(t('documentHistory'),rows(state.documentEntries||[],(x,i)=>`<div class="row"><span><strong>${esc(t(x.type))}</strong><br><small class="muted">${esc(x.date)} · ${esc(x.title)}</small><br><small>${esc(x.details)}</small></span><span class="row-actions"><button class="btn compact" data-doc-preview="${i}">${t('preview')}</button><button class="btn compact" data-doc-download="${i}">${t('download')}</button><button class="btn danger compact" data-remove="documentEntries" data-index="${i}">${t('delete')}</button></span></div>`,'noDocuments'))}</section>`}function stock(){return `<section>${head(t('stock'))}${card(t('inventory'),`<form id="stockForm" class="form"><input name="name" required placeholder="${t('product')}"><input name="qty" required type="number" step=".01" placeholder="${t('quantity')}"><input name="price" type="number" step=".01" placeholder="${t('purchasePrice')}"><input name="min" type="number" step=".01" min="0" placeholder="${t('minimumStock')}"><button class="btn primary">${t('add')}</button></form>${rows(state.stock,(x,i)=>editingRow('stock',i)?`<form class="form inline-edit" data-edit-form="stock" data-index="${i}"><input name="name" required value="${esc(x.name)}"><input name="qty" required type="number" step=".01" value="${esc(x.qty)}"><input name="price" type="number" step=".01" value="${esc(x.price)}"><input name="min" type="number" step=".01" min="0" value="${esc(x.min||0)}">${editButtons()}</form>`:`<div class="row"><span>${esc(x.name)}</span><span class="row-actions"><strong>${esc(x.qty)} · ${money(x.price)}</strong>${(+x.qty<=+(x.min||0))?`<span class="pill warning">${t('lowStock')}</span>`:''}${actions('stock',i)}</span></div>`,'noStock')}`)}</section>`}function suppliers(){return `<section>${head(t('suppliers'))}${card(t('suppliers'),`<form id="supplierForm" class="form"><input name="name" required placeholder="${t('supplierName')}"><input name="contact" placeholder="${t('contact')}"><button class="btn primary">${t('add')}</button></form>${rows(state.suppliers,(x,i)=>editingRow('suppliers',i)?`<form class="form inline-edit" data-edit-form="suppliers" data-index="${i}"><input name="name" required value="${esc(x.name)}"><input name="contact" value="${esc(x.contact)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.name)}</strong><br><small class="muted">${esc(x.contact)}</small></span>${actions('suppliers',i)}</div>`,'noSuppliers')}`)}</section>`}function team(){return `<section>${head(t('team'))}${card(t('team'),`<form id="teamForm" class="form"><input name="name" required placeholder="${t('employeeName')}"><input name="role" placeholder="${t('role')}"><button class="btn primary">${t('add')}</button></form>${rows(state.team,(x,i)=>editingRow('team',i)?`<form class="form inline-edit" data-edit-form="team" data-index="${i}"><input name="name" required value="${esc(x.name)}"><input name="role" value="${esc(x.role)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.name)}</strong><br><small class="muted">${esc(x.role)}</small></span>${actions('team',i)}</div>`,'noTeam')}`)}</section>`}function planning(){return `<section>${head(t('planning'))}${card(t('newShift'),`<form id="shiftForm" class="form"><input name="employee" required placeholder="${t('employeeName')}"><input name="date" required type="date"><input name="start" required type="time"><input name="end" required type="time"><button class="btn primary">${t('add')}</button></form>${rows(state.shifts,(x,i)=>editingRow('shifts',i)?`<form class="form inline-edit" data-edit-form="shifts" data-index="${i}"><input name="employee" required value="${esc(x.employee)}"><input name="date" required type="date" value="${esc(x.date)}"><input name="start" required type="time" value="${esc(x.start)}"><input name="end" required type="time" value="${esc(x.end)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.employee)}</strong><br><small class="muted">${esc(x.date)}</small></span><span class="row-actions"><strong>${esc(x.start)}–${esc(x.end)}</strong>${actions('shifts',i)}</span></div>`,'noShifts')}`)}</section>`}function recipes(){return `<section>${head(t('recipes'))}${card(t('newRecipe'),`<form id="recipeForm" class="form"><input name="name" required placeholder="${t('recipeName')}"><input name="cost" required type="number" step=".01" placeholder="${t('recipeCost')}"><input name="price" required type="number" step=".01" placeholder="${t('sellingPrice')}"><button class="btn primary">${t('add')}</button></form>${rows(state.recipes,(x,i)=>editingRow('recipes',i)?`<form class="form inline-edit" data-edit-form="recipes" data-index="${i}"><input name="name" required value="${esc(x.name)}"><input name="cost" required type="number" step=".01" value="${esc(x.cost)}"><input name="price" required type="number" step=".01" value="${esc(x.price)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.name)}</strong><br><small class="muted">${t('foodCost')} ${(x.price?x.cost/x.price*100:0).toFixed(1)}%</small></span><span class="row-actions"><strong>${money(x.cost)} / ${money(x.price)}</strong>${actions('recipes',i)}</span></div>`,'noRecipes')}`)}</section>`}function reservations(){return `<section>${head(t('reservations'))}${card(t('newReservation'),`<form id="reservationForm" class="form"><input name="name" required placeholder="${t('customer')}"><input name="time" required type="datetime-local"><input name="covers" required type="number" placeholder="${t('covers')}"><button class="btn primary">${t('add')}</button></form>${rows(state.reservations,(x,i)=>editingRow('reservations',i)?`<form class="form inline-edit" data-edit-form="reservations" data-index="${i}"><input name="name" required value="${esc(x.name)}"><input name="time" required type="datetime-local" value="${esc(x.time)}"><input name="covers" required type="number" value="${esc(x.covers)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.name)}</strong><br><small class="muted">${esc(x.time)}</small></span><span class="row-actions"><strong>${esc(x.covers)}</strong>${actions('reservations',i)}</span></div>`,'noReservations')}`)}</section>`}function customers(){return `<section>${head(t('customers'))}${card(t('newCustomer'),`<form id="customerForm" class="form"><input name="name" required placeholder="${t('customerName')}"><input name="phone" type="tel" placeholder="${t('phone')}"><input name="email" type="email" placeholder="${t('email')}"><input name="note" placeholder="${t('note')}"><button class="btn primary">${t('add')}</button></form>${rows(state.customers,(x,i)=>editingRow('customers',i)?`<form class="form inline-edit" data-edit-form="customers" data-index="${i}"><input name="name" required value="${esc(x.name)}"><input name="phone" type="tel" value="${esc(x.phone)}"><input name="email" type="email" value="${esc(x.email)}"><input name="note" value="${esc(x.note)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.name)}</strong><br><small class="muted">${[x.phone,x.email].filter(Boolean).map(esc).join(' · ')}</small>${x.note?`<br><small class="muted">${esc(x.note)}</small>`:''}</span>${actions('customers',i)}</div>`,'noCustomers')}`)}</section>`}function loyalty(){return `<section>${head(t('loyalty'))}${card(t('loyalty'),`<form id="loyaltyForm" class="form"><input name="name" required placeholder="${t('customerName')}"><input name="points" type="number" min="0" value="0" placeholder="${t('points')}"><button class="btn primary">${t('add')}</button></form>${rows(state.loyalty,(x,i)=>editingRow('loyalty',i)?`<form class="form inline-edit" data-edit-form="loyalty" data-index="${i}"><input name="name" required value="${esc(x.name)}"><input name="points" type="number" min="0" value="${esc(x.points)}">${editButtons()}</form>`:`<div class="row"><strong>${esc(x.name)}</strong><span class="row-actions"><span class="pill">${esc(x.points)} ${t('points')}</span>${actions('loyalty',i)}</span></div>`,'noLoyalty')}`)}</section>`}function incidents(){return `<section>${head(t('incidents'))}${card(t('newIncident'),`<form id="incidentForm" class="form"><input name="title" required placeholder="${t('incidentTitle')}"><input name="note" placeholder="${t('note')}"><button class="btn primary">${t('add')}</button></form>${rows(state.incidents,(x,i)=>editingRow('incidents',i)?`<form class="form inline-edit" data-edit-form="incidents" data-index="${i}"><input name="title" required value="${esc(x.title)}"><input name="note" value="${esc(x.note)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.title)}</strong><br><small class="muted">${esc(x.note)}</small></span>${actions('incidents',i)}</div>`,'noIncidents')}`)}</section>`}function waste(){return `<section>${head(t('waste'))}${card(t('newWaste'),`<form id="wasteForm" class="form"><input name="product" required placeholder="${t('product')}"><input name="qty" required type="number" step=".01" placeholder="${t('quantity')}"><button class="btn primary">${t('add')}</button></form>${rows(state.waste,(x,i)=>editingRow('waste',i)?`<form class="form inline-edit" data-edit-form="waste" data-index="${i}"><input name="product" required value="${esc(x.product)}"><input name="qty" required type="number" step=".01" value="${esc(x.qty)}">${editButtons()}</form>`:`<div class="row"><span>${esc(x.product)}</span><span class="row-actions"><strong>${esc(x.qty)}</strong>${actions('waste',i)}</span></div>`,'noWaste')}`)}</section>`}function purchases(){const total=(state.purchases||[]).reduce((s,x)=>s+(+x.amount||0),0);return `<section>${head(t('purchases'))}<div class="grid">${card(t('purchases'),`<div class="kpi">${money(total)}</div>`)}${card(t('suppliers'),`<div class="kpi">${state.suppliers.length}</div>`)}</div>${card(t('newPurchase'),`<form id="purchaseForm" class="form"><input name="supplier" required placeholder="${t('supplierName')}"><input name="date" required type="date" value="${today()}"><input name="amount" required type="number" step=".01" placeholder="${t('amount')}"><input name="note" placeholder="${t('note')}"><button class="btn primary">${t('add')}</button></form>${rows(state.purchases,(x,i)=>editingRow('purchases',i)?`<form class="form inline-edit" data-edit-form="purchases" data-index="${i}"><input name="supplier" required value="${esc(x.supplier)}"><input name="date" type="date" required value="${esc(x.date)}"><input name="amount" type="number" step=".01" required value="${esc(x.amount)}"><input name="note" value="${esc(x.note)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.supplier)}</strong><br><small class="muted">${esc(x.date)} · ${esc(x.note)}</small></span><span class="row-actions"><strong>${money(x.amount)}</strong>${actions('purchases',i)}</span></div>`,'noPurchases')}`)}</section>`}function products(){return `<section>${head(t('products'))}${card(t('products'),`<form id="productForm" class="form"><input name="name" required placeholder="${t('product')}"><select name="category"><option value="">${t('categories')}</option>${state.categories.map(x=>`<option value="${esc(x.name)}">${esc(x.name)}</option>`).join('')}</select><input name="cost" type="number" step=".01" placeholder="${t('purchasePrice')}"><input name="price" type="number" step=".01" placeholder="${t('sellingPrice')}"><button class="btn primary">${t('add')}</button></form>${rows(state.products,(x,i)=>editingRow('products',i)?`<form class="form inline-edit" data-edit-form="products" data-index="${i}"><input name="name" required value="${esc(x.name)}"><input name="category" value="${esc(x.category)}"><input name="cost" type="number" step=".01" value="${esc(x.cost)}"><input name="price" type="number" step=".01" value="${esc(x.price)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.name)}</strong><br><small class="muted">${esc(x.category)}</small></span><span class="row-actions"><strong>${money(x.price)}</strong>${actions('products',i)}</span></div>`,'noProducts')}`)}</section>`}function categories(){return `<section>${head(t('categories'))}${card(t('categories'),`<form id="categoryForm" class="form"><input name="name" required placeholder="${t('categoryName')}"><button class="btn primary">${t('add')}</button></form>${rows(state.categories,(x,i)=>editingRow('categories',i)?`<form class="form inline-edit" data-edit-form="categories" data-index="${i}"><input name="name" required value="${esc(x.name)}">${editButtons()}</form>`:`<div class="row"><strong>${esc(x.name)}</strong>${actions('categories',i)}</div>`,'noCategories')}`)}</section>`}function invoices(){const due=(state.invoices||[]).filter(x=>x.status!=='paid').reduce((s,x)=>s+(+x.amount||0),0);return `<section>${head(t('invoices'))}<div class="grid">${card(t('outstanding'),`<div class="kpi">${money(due)}</div>`)}${card(t('invoices'),`<div class="kpi">${state.invoices.length}</div>`)}</div>${card(t('newInvoice'),`<form id="invoiceForm" class="form"><input name="supplier" required placeholder="${t('supplierName')}"><input name="reference" required placeholder="${t('invoiceReference')}"><input name="date" type="date" value="${today()}" required><input name="amount" type="number" step=".01" required placeholder="${t('amount')}"><select name="status"><option value="pending">${t('pending')}</option><option value="paid">${t('paid')}</option></select><button class="btn primary">${t('add')}</button></form>${rows(state.invoices,(x,i)=>editingRow('invoices',i)?`<form class="form inline-edit" data-edit-form="invoices" data-index="${i}"><input name="supplier" required value="${esc(x.supplier)}"><input name="reference" required value="${esc(x.reference)}"><input name="date" type="date" required value="${esc(x.date)}"><input name="amount" type="number" step=".01" required value="${esc(x.amount)}"><select name="status"><option value="pending" ${x.status==='pending'?'selected':''}>${t('pending')}</option><option value="paid" ${x.status==='paid'?'selected':''}>${t('paid')}</option></select>${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.supplier)} · ${esc(x.reference)}</strong><br><small class="muted">${esc(x.date)} · ${t(x.status||'pending')}</small></span><span class="row-actions"><strong>${money(x.amount)}</strong>${actions('invoices',i)}</span></div>`,'noInvoices')}`)}</section>`}function recalls(){return `<section>${head(t('recalls'))}${card(t('recalls'),`<form id="recallForm" class="form"><input name="product" required placeholder="${t('product')}"><input name="lot" required placeholder="${t('lot')}"><input name="date" type="date" value="${today()}" required><select name="status"><option value="isolated">${t('isolated')}</option><option value="returned">${t('returned')}</option><option value="destroyed">${t('destroyed')}</option></select><textarea name="action" rows="3" placeholder="${t('actionTaken')}"></textarea><button class="btn primary">${t('add')}</button></form>${rows(state.recalls,(x,i)=>editingRow('recalls',i)?`<form class="form inline-edit" data-edit-form="recalls" data-index="${i}"><input name="product" required value="${esc(x.product)}"><input name="lot" required value="${esc(x.lot)}"><input name="date" type="date" required value="${esc(x.date)}"><select name="status"><option value="isolated" ${x.status==='isolated'?'selected':''}>${t('isolated')}</option><option value="returned" ${x.status==='returned'?'selected':''}>${t('returned')}</option><option value="destroyed" ${x.status==='destroyed'?'selected':''}>${t('destroyed')}</option></select><textarea name="action" rows="3">${esc(x.action)}</textarea>${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.product)} · ${t('lot')} ${esc(x.lot)}</strong><br><small class="muted">${esc(x.date)}</small><br><small>${esc(x.action)}</small></span><span class="row-actions"><span class="pill">${t(x.status||'isolated')}</span>${actions('recalls',i)}</span></div>`,'noRecalls')}`)}</section>`}function allergens(){return `<section>${head(t('allergens'))}${card(t('allergens'),`<form id="allergenForm" class="form"><input name="dish" required placeholder="${t('dish')}"><input name="allergens" required placeholder="${t('allergenList')}"><textarea name="note" rows="3" placeholder="${t('note')}"></textarea><button class="btn primary">${t('add')}</button></form>${rows(state.allergens,(x,i)=>editingRow('allergens',i)?`<form class="form inline-edit" data-edit-form="allergens" data-index="${i}"><input name="dish" required value="${esc(x.dish)}"><input name="allergens" required value="${esc(x.allergens)}"><textarea name="note" rows="3">${esc(x.note)}</textarea>${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.dish)}</strong><br><small>${esc(x.allergens)}</small><br><small class="muted">${esc(x.note)}</small></span><span class="row-actions">${actions('allergens',i)}</span></div>`,'noAllergens')}`)}</section>`}function deliveries(){return `<section>${head(t('deliveries'))}${card(t('deliveries'),`<form id="deliveryForm" class="form"><input name="supplier" required placeholder="${t('supplier')}"><input name="product" required placeholder="${t('product')}"><input name="date" type="date" value="${today()}" required><input name="temperature" type="number" step=".1" placeholder="${t('temperature')} °C"><input name="lot" placeholder="${t('lot')}"><select name="status"><option value="accepted">${t('accepted')}</option><option value="rejected">${t('rejected')}</option></select><button class="btn primary">${t('add')}</button></form>${rows(state.deliveries,(x,i)=>editingRow('deliveries',i)?`<form class="form inline-edit" data-edit-form="deliveries" data-index="${i}"><input name="supplier" required value="${esc(x.supplier)}"><input name="product" required value="${esc(x.product)}"><input name="date" type="date" required value="${esc(x.date)}"><input name="temperature" type="number" step=".1" value="${esc(x.temperature)}"><input name="lot" value="${esc(x.lot)}"><select name="status"><option value="accepted" ${x.status==='accepted'?'selected':''}>${t('accepted')}</option><option value="rejected" ${x.status==='rejected'?'selected':''}>${t('rejected')}</option></select>${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.product)}</strong><br><small>${esc(x.supplier)} · ${esc(x.lot)}</small><br><small class="muted">${esc(x.date)} · ${esc(x.temperature)}°C</small></span><span class="row-actions"><span class="pill">${t(x.status||'accepted')}</span>${actions('deliveries',i)}</span></div>`,'noDeliveries')}`)}</section>`}function cleaning(){return `<section>${head(t('cleaningPlan'))}${card(t('cleaningPlan'),`<form id="cleaningForm" class="form"><input name="area" required placeholder="${t('area')}"><input name="task" required placeholder="${t('cleaningTask')}"><select name="frequency"><option value="daily">${t('daily')}</option><option value="weekly">${t('weekly')}</option><option value="monthly">${t('monthly')}</option></select><input name="responsible" placeholder="${t('responsible')}"><button class="btn primary">${t('add')}</button></form>${rows(state.cleaning,(x,i)=>editingRow('cleaning',i)?`<form class="form inline-edit" data-edit-form="cleaning" data-index="${i}"><input name="area" required value="${esc(x.area)}"><input name="task" required value="${esc(x.task)}"><select name="frequency"><option value="daily" ${x.frequency==='daily'?'selected':''}>${t('daily')}</option><option value="weekly" ${x.frequency==='weekly'?'selected':''}>${t('weekly')}</option><option value="monthly" ${x.frequency==='monthly'?'selected':''}>${t('monthly')}</option></select><input name="responsible" value="${esc(x.responsible)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.area)} — ${esc(x.task)}</strong><br><small class="muted">${t(x.frequency||'daily')} · ${esc(x.responsible)}</small></span><span class="row-actions">${actions('cleaning',i)}</span></div>`,'noCleaning')}`)}</section>`}function audits(){return `<section>${head(t('audits'))}${card(t('audits'),`<form id="auditForm" class="form"><input name="date" type="date" value="${today()}" required><select name="type"><option value="internal">${t('internalAudit')}</option><option value="official">${t('officialInspection')}</option></select><input name="score" type="number" min="0" max="100" placeholder="${t('score')} %"><textarea name="actions" rows="3" placeholder="${t('correctiveActions')}"></textarea><button class="btn primary">${t('add')}</button></form>${rows(state.audits,(x,i)=>editingRow('audits',i)?`<form class="form inline-edit" data-edit-form="audits" data-index="${i}"><input name="date" type="date" required value="${esc(x.date)}"><select name="type"><option value="internal" ${x.type==='internal'?'selected':''}>${t('internalAudit')}</option><option value="official" ${x.type==='official'?'selected':''}>${t('officialInspection')}</option></select><input name="score" type="number" min="0" max="100" value="${esc(x.score)}"><textarea name="actions" rows="3">${esc(x.actions)}</textarea>${editButtons()}</form>`:`<div class="row"><span><strong>${t(x.type==='official'?'officialInspection':'internalAudit')}</strong><br><small class="muted">${esc(x.date)} · ${esc(x.score||0)}%</small><br><small>${esc(x.actions)}</small></span><span class="row-actions">${actions('audits',i)}</span></div>`,'noAudits')}`)}</section>`}function equipment(){return `<section>${head(t('equipmentRegister'))}${card(t('equipmentRegister'),`<form id="equipmentForm" class="form"><input name="name" required placeholder="${t('equipment')}"><input name="location" placeholder="${t('location')}"><input name="serviceDate" type="date"><select name="status"><option value="operational">${t('operational')}</option><option value="serviceDue">${t('serviceDue')}</option><option value="outOfService">${t('outOfService')}</option></select><button class="btn primary">${t('add')}</button></form>${rows(state.equipment,(x,i)=>editingRow('equipment',i)?`<form class="form inline-edit" data-edit-form="equipment" data-index="${i}"><input name="name" required value="${esc(x.name)}"><input name="location" value="${esc(x.location)}"><input name="serviceDate" type="date" value="${esc(x.serviceDate)}"><select name="status"><option value="operational" ${x.status==='operational'?'selected':''}>${t('operational')}</option><option value="serviceDue" ${x.status==='serviceDue'?'selected':''}>${t('serviceDue')}</option><option value="outOfService" ${x.status==='outOfService'?'selected':''}>${t('outOfService')}</option></select>${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.name)}</strong><br><small class="muted">${esc(x.location)} · ${esc(x.serviceDate||'')}</small></span><span class="row-actions"><span class="pill">${t(x.status||'operational')}</span>${actions('equipment',i)}</span></div>`,'noEquipment')}`)}</section>`}function leave(){return `<section>${head(t('leave'))}${card(t('leave'),`<form id="leaveForm" class="form"><input name="employee" required placeholder="${t('employeeName')}"><input name="start" type="date" required><input name="end" type="date" required><select name="status"><option value="requested">${t('requested')}</option><option value="approved">${t('approved')}</option><option value="refused">${t('refused')}</option></select><textarea name="note" rows="3" placeholder="${t('note')}"></textarea><button class="btn primary">${t('add')}</button></form>${rows(state.leave,(x,i)=>editingRow('leave',i)?`<form class="form inline-edit" data-edit-form="leave" data-index="${i}"><input name="employee" required value="${esc(x.employee)}"><input name="start" type="date" required value="${esc(x.start)}"><input name="end" type="date" required value="${esc(x.end)}"><select name="status"><option value="requested" ${x.status==='requested'?'selected':''}>${t('requested')}</option><option value="approved" ${x.status==='approved'?'selected':''}>${t('approved')}</option><option value="refused" ${x.status==='refused'?'selected':''}>${t('refused')}</option></select><textarea name="note" rows="3">${esc(x.note)}</textarea>${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.employee)}</strong><br><small class="muted">${esc(x.start)} → ${esc(x.end)}</small><br><small>${esc(x.note)}</small></span><span class="row-actions"><span class="pill">${t(x.status||'requested')}</span>${actions('leave',i)}</span></div>`,'noLeave')}`)}</section>`}function training(){return `<section>${head(t('training'))}${card(t('training'),`<form id="trainingForm" class="form"><input name="employee" required placeholder="${t('employeeName')}"><input name="topic" required placeholder="${t('trainingTopic')}"><input name="date" type="date" value="${today()}" required><select name="status"><option value="planned">${t('planned')}</option><option value="completed">${t('completed')}</option></select><button class="btn primary">${t('add')}</button></form>${rows(state.training,(x,i)=>editingRow('training',i)?`<form class="form inline-edit" data-edit-form="training" data-index="${i}"><input name="employee" required value="${esc(x.employee)}"><input name="topic" required value="${esc(x.topic)}"><input name="date" type="date" required value="${esc(x.date)}"><select name="status"><option value="planned" ${x.status==='planned'?'selected':''}>${t('planned')}</option><option value="completed" ${x.status==='completed'?'selected':''}>${t('completed')}</option></select>${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.employee)}</strong><br><small>${esc(x.topic)}</small><br><small class="muted">${esc(x.date)}</small></span><span class="row-actions"><span class="pill">${t(x.status||'planned')}</span>${actions('training',i)}</span></div>`,'noTraining')}`)}</section>`}function goals(){return `<section>${head(t('goals'))}${card(t('goals'),`<form id="goalForm" class="form"><select name="metric"><option value="revenue">${t('revenue')}</option><option value="covers">${t('covers')}</option><option value="avg">${t('avg')}</option></select><input name="target" type="number" step=".01" min="0" required placeholder="${t('target')}"><select name="period"><option value="day">${t('day')}</option><option value="week">${t('week')}</option><option value="month">${t('month')}</option></select><button class="btn primary">${t('save')}</button></form>${rows(state.goals,(x,i)=>`<div class="row"><span><strong>${t(x.metric)}</strong><br><small class="muted">${t(x.period)}</small></span><span class="row-actions"><strong>${x.metric==='covers'?esc(x.target):money(x.target)}</strong><button class="btn danger compact" data-remove="goals" data-index="${i}">${t('delete')}</button></span></div>`,'noGoals')}`)}</section>`}function alerts(){return `<section>${head(t('alerts'))}${card(t('alerts'),`<form id="alertForm" class="form"><select name="level"><option value="info">${t('info')}</option><option value="warning">${t('warning')}</option><option value="urgent">${t('urgent')}</option></select><input name="title" required placeholder="${t('alertTitle')}"><textarea name="note" rows="4" placeholder="${t('note')}"></textarea><button class="btn primary">${t('add')}</button></form>${rows(state.alerts,(x,i)=>editingRow('alerts',i)?`<form class="form inline-edit" data-edit-form="alerts" data-index="${i}"><input name="level" value="${esc(x.level)}"><input name="title" required value="${esc(x.title)}"><textarea name="note" rows="3">${esc(x.note)}</textarea>${editButtons()}</form>`:`<div class="row"><span><span class="pill status-${esc(x.level)}">${t(x.level||'info')}</span><br><strong>${esc(x.title)}</strong><br><small class="muted">${esc(x.note)}</small></span>${actions('alerts',i)}</div>`,'noAlerts')}`)}</section>`}function checklists(){return `<section>${head(t('checklists'))}${card(t('checklists'),`<form id="checklistForm" class="form"><select name="type"><option value="opening">${t('opening')}</option><option value="closing">${t('closing')}</option><option value="cleaning">${t('cleaning')}</option></select><input name="title" required placeholder="${t('checklistItem')}"><button class="btn primary">${t('add')}</button></form>${rows(state.checklists,(x,i)=>editingRow('checklists',i)?`<form class="form inline-edit" data-edit-form="checklists" data-index="${i}"><input name="type" value="${esc(x.type)}"><input name="title" required value="${esc(x.title)}">${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.title)}</strong><br><small class="muted">${t(x.type||'opening')}</small></span>${actions('checklists',i)}</div>`,'noChecklists')}`)}</section>`}function briefing(){return `<section>${head(t('briefing'))}${card(t('briefing'),`<form id="briefingForm" class="form"><input name="date" type="date" value="${today()}" required><input name="title" required placeholder="${t('briefingTitle')}"><textarea name="note" rows="5" required placeholder="${t('briefingNote')}"></textarea><button class="btn primary">${t('add')}</button></form>${rows(state.briefings,(x,i)=>editingRow('briefings',i)?`<form class="form inline-edit" data-edit-form="briefings" data-index="${i}"><input name="date" type="date" required value="${esc(x.date)}"><input name="title" required value="${esc(x.title)}"><textarea name="note" rows="4" required>${esc(x.note)}</textarea>${editButtons()}</form>`:`<div class="row"><span><strong>${esc(x.title)}</strong><br><small class="muted">${esc(x.date)}</small><br><small>${esc(x.note)}</small></span>${actions('briefings',i)}</div>`,'noBriefings')}`)}</section>`}function maintenance(){return `<section>${head(t('maintenance'))}${card(t('maintenance'),`<form id="maintenanceForm" class="form"><input name="equipment" required placeholder="${t('equipment')}"><input name="note" required placeholder="${t('note')}"><button class="btn primary">${t('add')}</button></form>${rows(state.maintenance,(x,i)=>`<div class="row"><span><strong>${esc(x.equipment)}</strong><br><small class="muted">${esc(x.note)}</small></span>${actions('maintenance',i)}</div>`,'noMaintenance')}`)}</section>`}function handover(){return `<section>${head(t('handover'))}${card(t('handover'),`<form id="handoverForm" class="form"><input name="title" required placeholder="${t('documentTitle')}"><textarea name="note" rows="5" required placeholder="${t('note')}"></textarea><button class="btn primary">${t('add')}</button></form>${rows(state.handover,(x,i)=>`<div class="row"><span><strong>${esc(x.title)}</strong><br><small class="muted">${esc(x.note)}</small></span>${actions('handover',i)}</div>`,'noHandover')}`)}</section>`}function more(){return `<section>${head(t('more'))}<div class="modules">${['orders','products','categories','planning','recipes','reservations','customers','incidents','waste','suppliers','purchases','invoices','team','recalls','allergens','deliveries','cleaning','audits','equipment','leave','training','goals','alerts','checklists','briefing','maintenance','handover','help','settings'].map(k=>`<button class="module" data-page="${k}"><strong>${t(k)}</strong></button>`).join('')}</div></section>`}function help(){return `<section>${head(t('help'))}<div class="modules">${['helpStart','helpOperations','helpHaccp','helpFinance','helpStock','helpTeam'].map(k=>`<article class="card help-card"><strong>${t(k)}</strong><p class="muted">${t(k+'Text')}</p></article>`).join('')}</div></section>`}function settings(){return `<section>${head(t('settings'))}${card(t('restaurantSettings'),`<form id="settingsForm" class="form"><input name="restaurant" value="${esc(state.preferences?.restaurant||'')}" placeholder="${t('restaurantName')}"><select name="currency">${['CHF','EUR','USD','GBP'].map(x=>`<option value="${x}" ${x===(state.preferences?.currency||'CHF')?'selected':''}>${x}</option>`).join('')}</select><select name="theme"><option value="taupe" ${state.preferences?.theme==='taupe'?'selected':''}>${t('themeTaupe')}</option><option value="light" ${state.preferences?.theme==='light'?'selected':''}>${t('themeLight')}</option><option value="dark" ${state.preferences?.theme==='dark'?'selected':''}>${t('themeDark')}</option></select><button class="btn primary">${t('save')}</button></form>`)}${card(t('languageLabel'),`<select id="settingsLang" class="lang">${LANGS.map(x=>`<option value="${x}" ${x===language()?'selected':''}>${x.toUpperCase()}</option>`).join('')}</select>`)}${card(t('dataManagement'),`<div class="actions"><button id="exportData" class="btn">${t('exportData')}</button><button id="resetData" class="btn danger">${t('resetData')}</button></div>`)}</section>`}function view(){return({dashboard,operations,orders,products,haccp,finance,documents,stock,suppliers,purchases,invoices,categories,team,planning,recipes,reservations,customers,loyalty,incidents,waste,recalls,allergens,deliveries,cleaning,audits,equipment,leave,training,goals,alerts,checklists,briefing,maintenance,handover,help,more,settings}[page]||dashboard)()}
function render(){const l=language();document.documentElement.lang=l;document.documentElement.dataset.theme=state.preferences?.theme||'taupe';document.getElementById('app').innerHTML=`<main class="shell"><header class="top"><div><div class="brand"><img src="assets-remaprohub-logo.png" alt="ReMaPro Hub"></div><div class="muted">${t('account')}</div></div><select id="lang" class="lang">${LANGS.map(x=>`<option value="${x}" ${x===l?'selected':''}>${x.toUpperCase()}</option>`).join('')}</select></header>${view()}</main><nav class="nav">${[['dashboard','dashboard'],['operations','operations'],['finance','finance'],['documents','documents'],['more','more']].map(([p,k])=>`<button data-page="${p}" class="${page===p?'active':''}">${t(k)}</button>`).join('')}</nav>`;bind()}
function patchFor(collection,d){if(collection==='products')return{name:d.get('name'),category:d.get('category'),cost:+d.get('cost'),price:+d.get('price')};if(collection==='categories')return{name:d.get('name')};if(collection==='orders')return{reference:d.get('reference'),amount:+d.get('amount'),status:d.get('status')};if(collection==='loyalty')return{name:d.get('name'),points:+d.get('points')};if(collection==='customers')return{name:d.get('name'),phone:d.get('phone'),email:d.get('email'),note:d.get('note')};if(collection==='suppliers')return{name:d.get('name'),contact:d.get('contact')};if(collection==='team')return{name:d.get('name'),role:d.get('role')};if(collection==='temps')return{equipment:d.get('equipment'),value:d.get('value')};if(collection==='stock')return{name:d.get('name'),qty:+d.get('qty'),price:+d.get('price')||0,min:+d.get('min')||0};if(collection==='shifts')return{employee:d.get('employee'),date:d.get('date'),start:d.get('start'),end:d.get('end')};if(collection==='recipes')return{name:d.get('name'),cost:+d.get('cost'),price:+d.get('price')};if(collection==='reservations')return{name:d.get('name'),time:d.get('time'),covers:+d.get('covers')};if(collection==='incidents')return{title:d.get('title'),note:d.get('note')};if(collection==='waste')return{product:d.get('product'),qty:+d.get('qty')};if(collection==='invoices')return{supplier:d.get('supplier'),reference:d.get('reference'),date:d.get('date'),amount:+d.get('amount'),status:d.get('status')};if(collection==='purchases')return{supplier:d.get('supplier'),date:d.get('date'),amount:+d.get('amount'),note:d.get('note')};if(collection==='recalls')return{product:d.get('product'),lot:d.get('lot'),date:d.get('date'),status:d.get('status'),action:d.get('action')};if(collection==='allergens')return{dish:d.get('dish'),allergens:d.get('allergens'),note:d.get('note')};if(collection==='deliveries')return{supplier:d.get('supplier'),product:d.get('product'),date:d.get('date'),temperature:d.get('temperature'),lot:d.get('lot'),status:d.get('status')};if(collection==='cleaning')return{area:d.get('area'),task:d.get('task'),frequency:d.get('frequency'),responsible:d.get('responsible')};if(collection==='audits')return{date:d.get('date'),type:d.get('type'),score:+d.get('score')||0,actions:d.get('actions')};if(collection==='equipment')return{name:d.get('name'),location:d.get('location'),serviceDate:d.get('serviceDate'),status:d.get('status')};if(collection==='leave')return{employee:d.get('employee'),start:d.get('start'),end:d.get('end'),status:d.get('status'),note:d.get('note')};if(collection==='training')return{employee:d.get('employee'),topic:d.get('topic'),date:d.get('date'),status:d.get('status')};if(collection==='alerts')return{level:d.get('level'),title:d.get('title'),note:d.get('note')};if(collection==='checklists')return{type:d.get('type'),title:d.get('title')};if(collection==='briefings')return{date:d.get('date'),title:d.get('title'),note:d.get('note')};if(collection==='maintenance')return{equipment:d.get('equipment'),note:d.get('note')};if(collection==='handover')return{title:d.get('title'),note:d.get('note')};return{}}
function bind(){document.getElementById('lang')?.addEventListener('change',e=>setLanguage(e.target.value));document.getElementById('settingsLang')?.addEventListener('change',e=>setLanguage(e.target.value));document.getElementById('settingsForm')?.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.target);updatePreferences(state,{restaurant:d.get('restaurant')||'',currency:d.get('currency')||'CHF',theme:d.get('theme')||'taupe'});render()});document.getElementById('exportData')?.addEventListener('click',()=>{const blob=new Blob([exportData(state)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`remaprohub-v27-${today()}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),0)});document.getElementById('resetData')?.addEventListener('click',()=>{if(confirm(t('resetConfirm'))){resetState(state);editing=null;render()}});document.querySelectorAll('[data-doc-fill]').forEach(b=>b.addEventListener('click',()=>{editing={collection:'document',type:b.dataset.docFill};render()}));document.querySelectorAll('[data-doc-preview]').forEach(b=>b.addEventListener('click',()=>{const x=state.documentEntries?.[+b.dataset.docPreview];if(!x)return;editing={collection:'document',type:x.type};const el=document.querySelector('[data-doc-preview="'+b.dataset.docPreview+'"]')?.closest('.row');if(el)el.querySelector('small:last-of-type')?.scrollIntoView({behavior:'smooth',block:'center'})}));document.querySelectorAll('[data-doc-download]').forEach(b=>b.addEventListener('click',()=>{const x=state.documentEntries?.[+b.dataset.docDownload];if(!x)return;const w=window.open('','_blank');if(!w)return;const safe=v=>esc(v),restaurant=safe(state.preferences?.restaurant||'ReMaPro Hub');w.document.write(`<!doctype html><html lang="${language()}"><head><meta charset="utf-8"><title>${safe(t(x.type))}</title><style>body{font-family:Arial,sans-serif;color:#2b241d;margin:36px;line-height:1.5}header{border-bottom:2px solid #7a4b2a;padding-bottom:16px;margin-bottom:24px}h1{margin:0 0 6px}h2{margin-top:28px}.meta{color:#756b61}.details{white-space:pre-wrap;border:1px solid #e8ddd0;border-radius:12px;padding:18px}@media print{button{display:none}body{margin:18mm}}</style></head><body><header><h1>${restaurant}</h1><div class="meta">ReMaPro Hub</div></header><h2>${safe(t(x.type))}</h2><p class="meta">${safe(x.date)} · ${safe(x.title)}</p><div class="details">${safe(x.details)}</div><p><button onclick="window.print()">${safe(t('printPdf'))}</button></p></body></html>`);w.document.close();setTimeout(()=>w.print(),250)}));document.querySelectorAll('[data-page]').forEach(b=>b.addEventListener('click',()=>{editing=null;page=b.dataset.page;render()}));document.querySelectorAll('[data-period]').forEach(b=>b.addEventListener('click',()=>{period=b.dataset.period;render()}));document.querySelectorAll('[data-task]').forEach(x=>x.addEventListener('change',()=>{state.tasks[+x.dataset.task][1]=x.checked;persist()}));document.querySelectorAll('[data-edit]').forEach(b=>b.addEventListener('click',()=>{editing={collection:b.dataset.edit,index:+b.dataset.index};render()}));document.querySelectorAll('[data-cancel-edit]').forEach(b=>b.addEventListener('click',()=>{editing=null;render()}));document.querySelectorAll('[data-edit-form]').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const collection=f.dataset.editForm,index=+f.dataset.index,d=new FormData(f);if((collection==='orders'?updateOrder(state,index,patchFor(collection,d)):collection==='purchases'?updatePurchase(state,index,patchFor(collection,d)):updateRecord(state,collection,index,patchFor(collection,d))){editing=null;persist();render()}}));document.querySelectorAll('[data-remove]').forEach(b=>b.addEventListener('click',()=>{if((b.dataset.remove==='orders'?removeOrder(state,+b.dataset.index):b.dataset.remove==='purchases'?removePurchase(state,+b.dataset.index):removeRecord(state,b.dataset.remove,+b.dataset.index))){editing=null;persist();render()}}));const form=(id,fn)=>document.getElementById(id)?.addEventListener('submit',e=>{e.preventDefault();fn(new FormData(e.target));persist();render()});form('productForm',f=>state.products.unshift({name:f.get('name'),category:f.get('category'),cost:+f.get('cost'),price:+f.get('price')}));form('categoryForm',f=>state.categories.unshift({name:f.get('name')}));form('orderForm',f=>recordOrder(state,{reference:f.get('reference'),amount:f.get('amount'),status:f.get('status'),date:today()}));form('documentForm',f=>{saveDocument(state,{type:f.get('type'),date:f.get('date'),title:f.get('title'),details:f.get('details')});editing=null});form('financeForm',f=>recordFinance(state,{date:f.get('date'),revenue:f.get('revenue'),covers:f.get('covers'),expenses:f.get('expenses')}));form('tempForm',f=>state.temps.unshift({equipment:f.get('equipment'),value:f.get('value')}));form('stockForm',f=>state.stock.unshift({name:f.get('name'),qty:+f.get('qty'),price:+f.get('price')||0,min:+f.get('min')||0}));form('supplierForm',f=>state.suppliers.unshift({name:f.get('name'),contact:f.get('contact')}));form('teamForm',f=>state.team.unshift({name:f.get('name'),role:f.get('role')}));form('shiftForm',f=>state.shifts.unshift({employee:f.get('employee'),date:f.get('date'),start:f.get('start'),end:f.get('end')}));form('recipeForm',f=>state.recipes.unshift({name:f.get('name'),cost:+f.get('cost'),price:+f.get('price')}));form('reservationForm',f=>state.reservations.unshift({name:f.get('name'),time:f.get('time'),covers:+f.get('covers')}));form('loyaltyForm',f=>state.loyalty.unshift({name:f.get('name'),points:+f.get('points')}));form('customerForm',f=>state.customers.unshift({name:f.get('name'),phone:f.get('phone'),email:f.get('email'),note:f.get('note')}));form('incidentForm',f=>state.incidents.unshift({title:f.get('title'),note:f.get('note')}));form('wasteForm',f=>state.waste.unshift({product:f.get('product'),qty:+f.get('qty')}));form('invoiceForm',f=>state.invoices.unshift({supplier:f.get('supplier'),reference:f.get('reference'),date:f.get('date'),amount:+f.get('amount'),status:f.get('status')}));form('purchaseForm',f=>recordPurchase(state,{supplier:f.get('supplier'),date:f.get('date'),amount:f.get('amount'),note:f.get('note')}));form('recallForm',f=>state.recalls.unshift({product:f.get('product'),lot:f.get('lot'),date:f.get('date'),status:f.get('status'),action:f.get('action')}));form('allergenForm',f=>state.allergens.unshift({dish:f.get('dish'),allergens:f.get('allergens'),note:f.get('note')}));form('deliveryForm',f=>state.deliveries.unshift({supplier:f.get('supplier'),product:f.get('product'),date:f.get('date'),temperature:f.get('temperature'),lot:f.get('lot'),status:f.get('status')}));form('cleaningForm',f=>state.cleaning.unshift({area:f.get('area'),task:f.get('task'),frequency:f.get('frequency'),responsible:f.get('responsible')}));form('auditForm',f=>state.audits.unshift({date:f.get('date'),type:f.get('type'),score:+f.get('score')||0,actions:f.get('actions')}));form('equipmentForm',f=>state.equipment.unshift({name:f.get('name'),location:f.get('location'),serviceDate:f.get('serviceDate'),status:f.get('status')}));form('leaveForm',f=>state.leave.unshift({employee:f.get('employee'),start:f.get('start'),end:f.get('end'),status:f.get('status'),note:f.get('note')}));form('trainingForm',f=>state.training.unshift({employee:f.get('employee'),topic:f.get('topic'),date:f.get('date'),status:f.get('status')}));form('goalForm',f=>state.goals.unshift({metric:f.get('metric'),target:+f.get('target'),period:f.get('period')}));form('alertForm',f=>state.alerts.unshift({level:f.get('level'),title:f.get('title'),note:f.get('note')}));form('checklistForm',f=>state.checklists.unshift({type:f.get('type'),title:f.get('title')}));form('briefingForm',f=>state.briefings.unshift({date:f.get('date'),title:f.get('title'),note:f.get('note')}));form('maintenanceForm',f=>state.maintenance.unshift({equipment:f.get('equipment'),note:f.get('note')}));form('handoverForm',f=>state.handover.unshift({title:f.get('title'),note:f.get('note')}))}window.addEventListener('rmp:language',render);render();